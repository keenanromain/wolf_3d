/*
 *Main first calls validate to ensure proper input
 *Then it parses the input into our data structures
 *From there it begins to process the parsed input
 * once processed, we go into render before we drae
 * */
